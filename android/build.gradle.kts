// Top-level build file. Versions pinned to what's already cached on this machine
// (Gradle 9.0.0, AGP 8.12.0, Kotlin 2.1.20) so builds are fast and offline-friendly.
plugins {
    id("com.android.application") version "8.12.0" apply false
    id("org.jetbrains.kotlin.android") version "2.1.20" apply false
}
